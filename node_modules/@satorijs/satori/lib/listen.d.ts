/// <reference types="node" />
import { Server } from 'net';
export interface ListenOptions {
    host?: string;
    port?: number;
    maxPort?: number;
}
export declare function listen(server: Server, { host, port, maxPort }: ListenOptions): Promise<number>;
