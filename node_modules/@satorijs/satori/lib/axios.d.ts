import { Agent } from 'agent-base';
declare type CreateAgent = (opts: string) => Agent;
export declare function register(protocols: string[], callback: CreateAgent): void;
export declare function getAgent(url: string): Agent;
export {};
